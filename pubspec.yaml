name: camerapick
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

assets:
  - image/idbdVJ3dte.jpeg

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^0.8.4+3
  get: ^4.3.8
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
